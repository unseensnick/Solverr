{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "schedule": ["before 6am on monday"],
  "labels": ["dependencies"],
  "packageRules": [
    {
      "description": "Group Python requirement bumps into a single PR",
      "matchManagers": ["pip_requirements"],
      "groupName": "python dependencies"
    },
    {
      "description": "Do not auto-merge: no PR-gating tests, and a bad browser/driver bump can silently break solving. Review each PR.",
      "matchPackageNames": ["*"],
      "automerge": false
    }
  ]
}
