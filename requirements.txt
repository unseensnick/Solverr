bottle==0.13.4
waitress==3.0.2
selenium==4.39.0
func-timeout==4.3.5
prometheus-client==0.23.1
# Required by undetected_chromedriver
requests==2.32.5
certifi==2025.11.12
websockets==15.0.1
packaging==25.0
# Only required for Linux and macOS
xvfbwrapper==0.2.16; platform_system != "Windows"
# Only required for Windows
pefile==2024.8.26; platform_system == "Windows"
# Stealth engine (Camoufox + playwright-captcha), mirrors Byparr's stack
playwright==1.60.*
playwright-captcha==0.1.*
# Pinned to a commit: an unpinned git dep makes every image rebuild a lottery,
# and a bad bump here breaks solving while the build stays green.
invisible_playwright @ git+https://github.com/feder-cr/invisible_playwright.git@a1b75d93e928c37130833d667ae1acf15a23d027
