# Deep-audit ledger

Findings a `/deep-audit` run refuted, so the next run does not raise them again. A row holds only while
its reason is still true; once the code or the ruling behind it changes, the finding may come back with
the new evidence. Rows are appended by the skill and committed by the owner.

| Range | Location | Claim | Refuted because |
|---|---|---|---|
| 237faf1...deabac9 | src/engines/chrome_engine.py:371 | The tabs_till_verify loop keeps pressing Tab+Space on the destination page until the deadline once the widget is gone | The two mechanisms the finding blames are both recorded deliberate divergences, not oversights. |
| 237faf1...deabac9 | src/engines/chrome_engine.py:152 | The Turnstile deadline ignores waitInSeconds, so a request that gets no token can still turn into a timeout error | Refuted: the arithmetic is real but it is not a Turnstile-path defect, it is the inherited whole-request budget semantic, and it is recorded as deliberate. 1. Mislocated. |
| 237faf1...deabac9 | src/sessions.py:175 | end_use does not refresh last_used, so a session is judged idle from the start of its last request and can be reaped right after one finishes | The mechanism is real but it is not a defect, and the proposed fix turns the suite red. |
| 237faf1...deabac9 | src/config.py:124 | The config docstring and README say an explicit 'auto' timezone equals unset, but the code treats them differently | The behaviour difference is real but deliberate, documented, and pinned; the finding's defect and its failure scenario do not survive. The code is intentional and tested. |
| 237faf1...deabac9 | src/passthrough.py:276 | A stalled owner client keeps the in-flight slot claimed, contrary to 5b79fb1's 'always release' | The stated failure ("waits 120 s and gets 502 solver error, although the solved body is sitting in pending.body") does not happen. |
| 237faf1...deabac9 | src/engines/stealth_engine.py:191 | SOCKS proxy credentials sit in plaintext in the stealth browser's temp profile for its whole lifetime | Both halves fail, for different reasons. Half 1 (SOCKS credentials in the temp profile). |
| 237faf1...deabac9 | src/budget.py:30 | SOLVE_MARGIN_SECONDS does not cover waitInSeconds, so a solve that runs to its deadline turns into a timeout error | Refuted as stated. |
| 237faf1...deabac9 | src/pipeline.py:61 | pipeline.approach forces the stealth engine to navigate twice when cookies are sent, though Playwright cookies need no origin | The mechanics are accurate but the classification is not: the second navigation is deliberate, ruled shared, and pinned by a passing gate for both engines. 1. |
| 237faf1...deabac9 | src/budget.py:16 | SOLVE_MARGIN_SECONDS does not cover the work both engines do after the deadline: waitInSeconds, and stealth's post-solve settle of up to 10s | The arithmetic gap is real but it is neither new in the range nor a defect as framed, and the Chrome half of the failure scenario is mechanically wrong. 1) The range did not change this behaviour. |
| 237faf1...deabac9 | src/detection.py:66 | The challenge-platform beacon selector turns a clean page into a challenge that can never clear, not 'a redundant solve attempt' | The code trace holds but the premise does not. |
| 237faf1...deabac9 | src/flaresolverr_service.py:252 | _DOMAIN_ENGINE grows by one entry per distinct host forever, the same defect 524eb0b capped in the metrics labels | The mechanism is real but it is an explicitly known, owner-acknowledged item that the repo config tells reviewers not to re-report, and nothing in the audited range introduced or worsened it. |
| 237faf1...deabac9 | src/sessions.py:153 | get()'s expiry path removes whatever sits under the id, not the session it checked, so it can orphan a browser nobody will ever close | The mechanism is real but the window is not. |
| 237faf1...deabac9 | src/config.py:47 | STEALTH_START_TIMEOUT of 0 or less makes every Camoufox launch time out instantly | The mechanism reproduces, but it is the setting doing what it says, not a defect. |
| 237faf1...deabac9 | src/config.py:193 | A non-integer BROWSER_WAIT_TIMEOUT is silently replaced by 1, the same 'tells nobody' failure its docstring says the fallback fixes | Recorded as deliberate, and the ledger's premise still holds. |
| 237faf1...deabac9 | src/pipeline.py:87 | pipeline.verdict keeps an is_turnstile variable that can never be True, left over from the stealth _detect it replaced | The factual half of the claim holds but it is not a defect. |
| 237faf1...deabac9 | src/utils.py:240 | get_webdriver's finally clears proxy_extension_dir after the last read, a dead store added by the cleanup move | The factual half checks out but it is not a defect. |
| 237faf1...deabac9 | src/engine_fakes.py:109 | The Chrome fake driver's execute_script, execute_cdp_cmd and switch_to stubs are never reached by any test, and the Cookie alias is never referenced | The coverage data reproduces exactly, but the "dead" label does not survive it, and two of the four items are wrong as stated. 1. `execute_cdp_cmd` is live-reachable, not dead. |
| 237faf1...deabac9 | src/flaresolverr_service.py:299 | _validate_session_ttl's type checks are unreachable on /v1 since e260d9c made annotations binding | The mechanical half reproduces, but neither half is a defect. |
| 237faf1...deabac9 | src/flaresolverr_service.py:486 | The 'All engines failed to solve the challenge.' fallback in _resolve_challenge can never be raised | The unreachability is factually right but it is not a defect. |
| 237faf1...deabac9 | src/flaresolverr_service.py:403 | _engine_plan's last-resort primary and _pool_has's False return are unreachable | Half the claim is wrong and the other half is a two-line no-op guard, so the finding as stated is not a defect. The _pool_has half does not hold. |
| 237faf1...deabac9 | src/passthrough.py:44 | passthrough keeps a stale 120000 ms timeout default that production never reads and config no longer agrees with | The mechanics are as described, but nothing that could be called a defect survives. |
| 237faf1...deabac9 | src/passthrough.py:264 | The passthrough cache gate's 2xx status check can never fail, and its comment still says the engines hardcode the status | Refuted. The comment is deliberate documentation of an intentionally inert clause, and its operative sentence is still true. 1. The failure scenario contradicts the comment it cites. |
| 237faf1...deabac9 | src/engines/chrome_engine.py:230 | The PDF body capability is a nullable field plus a README note, never routed or refused | The facts are right but the rule does not bite, on four grounds. 1. The gap predates the law. |
| 237faf1...deabac9 | src/engines/stealth_engine.py:300 | Session acquire/release in solve() is an unpinned hand-written pair: removing the proxy or end_use on either engine leaves the whole suite green | No present defect, and the rule it cites does not say what the finding says. |
| 237faf1...deabac9 | src/engines/chrome_engine.py:91 | The Windows close-then-quit teardown got a helper, but the Chrome engine keeps its own inline copy | The duplication is real but it is neither a defect nor the rule violation as charged. |
| 237faf1...deabac9 | src/flaresolverr_service.py:12 | A module constant sits in the middle of the controller's import block | The rule it cites is satisfied, and the failure scenario cannot occur in this repo. `.claude/rules/code-quality.md` says "Imports grouped: standard library, third-party, local. |
| 237faf1...deabac9 | src/config.py:132 | Boolean config readers lack the is_/has_/should_ prefix; response_headers() reads as returning headers | The cited lines are accurate (`response_headers() -> bool` at src/config.py:132-148, and the unprefixed predicates at src/config.py:18, 28, 54, 223, 259), and the whole file is new in the range (`git cat-file -e 237faf1:src/config.py` fails: the file does not exist at the base). |
| 237faf1...deabac9 | src/passthrough.py:293 | passthrough.py puts its public entry point after every helper | The finding's premise is factually wrong and its rule reading is contradicted by the repo's own convention. |
| 237faf1...deabac9 | src/engines/stealth_engine.py:361 | Stealth adapter's settle waits catch every exception, log it as "timed out", and drop it without exc_info | No rule is violated and the failure scenario does not survive the call chain. 1. The rule the finding invokes does not cover these sites. `.claude/rules/error-handling.md:10` reads: "Don't swallow errors silently. |
| 237faf1...deabac9 | src/config.py:176 | Numeric config readers swallow a malformed value's ValueError with no log at all | The mechanics are right (src/config.py:172-177, 39-42, 47-51 return the default with no log; I read all three), but nothing here is a defect or a rule violation. The rule cited does not cover this code. |
| 237faf1...deabac9 | src/passthrough.py:288 | Passthrough's log_message override also silences http.server's error log, so 400/501 answers are never logged | The mechanism is real but it is not the rule violation the finding claims, and it is not a swallowed error. |
| 237faf1...deabac9 | src/engines/stealth_engine.py:392 | The stealth engine's own 'Navigating to...' debug line logs the target URL unredacted, beside the request log that redacts it | The factual half checks out: `uv run --no-project python -c` over `redact.body({'url':'https://user:pw@example-site.tld/'})` returns `https://user:<redacted>@example-site.tld/` (src/redact.py:58-59 routes the `url` key through `redact.url`, src/redact.py:17-30), while src/engines/stealth_engine.py:392 logs `f"Navigating to... |
| 237faf1...deabac9 | src/test_engine_conformance.py:64 | test_an_ordinary_solve_reports_an_empty_header_map depends on the ambient RESPONSE_HEADERS and restates a rule already pinned | The reproduction is real but the rule violations it rests on are misreadings, and the project's own gate passes. |
| 237faf1...deabac9 | src/assembly.py:58 | solution.url being the final page rather than the requested URL is pinned by no test | Claim 2 is wrong, and claim 1 is true as a fact but is not the rule violation it is stated as. Claim 2 (refuted by execution). |
| 237faf1...deabac9 | src/test_proxy_extension.py:40 | The 'successful launch' proxy-extension test actually runs a launch that raises, and the test swallows the error | The mechanical half reproduces, the consequence does not. |
| 237faf1...deabac9 | src/test_postform.py:37 | test_postform checks the action attribute before the data: URL decode the module says the browser applies, which hides a mangled POST target | The finding as filed is about the test, and the test holds. |
| 237faf1...deabac9 | src/test_geo.py:370 | Nothing pins that both engines get the same timezone and language, though one test's name says it does | The law clause the finding rests on is satisfied by the top rung, not violated. |
| 237faf1...deabac9 | src/test_browser_identity.py:70 | The warn-once LANG test depends on process-global state and fails on a second run | The mechanism is real but nothing in the project can hit it, so it is not a flaky test. |
| 237faf1...deabac9 | src/test_response_shape.py:90 | Stealth-only cookie-dialect tests superseded by the conformance cases were kept | The stated failure scenario cannot happen, and the deletion the law asks for was already done for the tests it names. |
| 237faf1...deabac9 | Dockerfile:103 | The image healthcheck probes 127.0.0.1 but the server binds HOST, so HOST=:: or a specific interface makes a serving container report unhealthy | Out of the audited slice, and the failure scenario does not hold. |
| 237faf1...deabac9 | Dockerfile:82 | The published image ships FlareSolverr-, Byparr- and undetected-chromedriver-derived code with no LICENSE or NOTICE | Out of the audited range and not a change this range made, on top of being a legal question rather than a code defect. The audit range is 237faf1...deabac9 limited to src/. The finding's anchor, Dockerfile:82, is not in src/. |
| 237faf1...deabac9 | src/dtos.py:85 | The headers fix exempted one deprecated field and left three siblings type-checked | The finding misstates the premise of the headers exemption. |
| 237faf1...deabac9 | src/engines/stealth_engine.py:471 | tabs_till_verify is still a silent no-op on stealth, and turnstile_token semantics now differ by engine | The code facts hold but the defect does not. |
| 237faf1...deabac9 | src/engines/stealth_engine.py:446 | A Turnstile checkbox that never yields a token returns 200 on Chrome but an error on stealth | The outcome divergence is real but it is the sanctioned capability boundary, and it does not come from the cited commit. |
| 237faf1...deabac9 | src/engines/chrome_engine.py:198 | The 1.4.0 fix that stops a solve returning a between-rounds challenge page landed on stealth only; Chrome still trusts its first clear reading, and... | The factual half checks out, the rule half does not. |
| 237faf1...deabac9 | src/engines/chrome_engine.py:283 | An invalid BROWSER_TIMEZONE is never validated, and Chrome drops it silently at debug level | Refuted on three executed grounds. |
| 237faf1...deabac9 | src/engines/chrome_engine.py:358 | An unsolved Turnstile widget fails over on stealth but comes back as a 200 with a null token on Chrome, and pins Chrome for the host | Refuted on three independent grounds, two of them factual errors in the finding. 1. The asymmetry is a ruled capability boundary, not drift. |
| 237faf1...deabac9 | src/engines/stealth_engine.py:407 | tabs_till_verify is still silently ignored by the stealth engine, the counter-example the law names | Refuted: the state is the owner's, re-ruled after the range, and the client-observable half already has parity. |
| 237faf1...deabac9 | src/engines/chrome_engine.py:182 | A challenge that ends on an access-denied page is refused by stealth but returned as "Challenge solved!" by Chrome | The asymmetry reproduces, but it sits inside the one surface the law declines, on lines the range never changed, and the doc it cites already records the second half. |
| 237faf1...deabac9 | src/detection.py:33 | The challenge-platform script selector added for locale-proof detection makes a clean beacon-carrying page cost the whole solve budget on both engines | The call chain is real but the premise fails. |
| 237faf1...deabac9 | src/utils.py:107 | Proxy credentials go into the Chrome auth extension's JavaScript unescaped | The escaping gap is real but is not a finding for this range, and its stated justification does not hold. |
| 237faf1...deabac9 | src/dtos.py:79 | waitInSeconds is not bounded by the budget it has to fit in, so a completed solve is thrown away | The mechanism is real but it is upstream's, and the halving that sharpens it is a measured, documented decision with a documented remedy. |
| 237faf1...deabac9 | src/flaresolverr_service.py:324 | maxTimeout=Infinity and an oversized session_ttl_minutes slip past the new validators and fail with raw OverflowErrors | The mechanics reproduce exactly as described, but they are not a defect against either validator's contract; the observable difference is the wording of an error message for two absurd inputs, with no behavioural or resource consequence. |
| 237faf1...deabac9 | src/session_reaper.py:31 | The reaper's startup line reports a disabled TTL or cap as zero, which reads as the most aggressive setting | The line reports the configured value exactly, so the claimed equivalence to ffa40fc fails. |
| 237faf1...deabac9 | src/engines/chrome_engine.py:128 | Both engines' 'Navigating to...' debug lines log the request URL unredacted, though the body log redacts its userinfo | Mechanism confirmed, defect not. |
| 237faf1...deabac9 | src/config.py:1 | Byparr's RETURN_ONLY_COOKIES server-wide default has no Solverr reader, and the drop is not recorded | The factual core holds (no Solverr module reads RETURN_ONLY_COOKIES: the only env readers in src/ are src/config.py:15-275, src/flaresolverr.py:16-94, src/geo.py:325 and src/utils.py:28-36, none of which names it), but it is not a defect. |
| 237faf1...deabac9 | src/test_log_redaction.py:21 | test_log_redaction stubs the handler, so no test can see an unvalidated or list-headers request | The finding's stated mechanism is false, and its two supporting sub-claims are false. 1) The stub does not block anything. |
| 237faf1...deabac9 | src/flaresolverr_service.py:450 | The minimum-viable-share floor is applied to the fallback only, so the first engine of a two-engine plan is routinely handed a share the controller... | The split's consequence at small `maxTimeout` is a recorded, documented tradeoff, and the failure scenario's mechanics are wrong for the engine it names. 1. The stated failure mechanism does not hold for Chrome. |
| 237faf1...deabac9 | src/assembly.py:66 | waitInSeconds is spent inside the engine's timeout, is never subtracted from the solve deadline and has no ceiling, so a solved page comes back as ... | The three mechanical sub-claims check out, but the defect does not stand as stated. |
| 237faf1...deabac9 | src/engine_fakes.py:183 | The cookie conformance tests pin the dialect but cannot see the scope, because both fakes hardcode the same single domain | The finding's stated failure scenario is executed-false. |
| 237faf1...deabac9 | src/engines/stealth_engine.py:96 | The stealth cookie output has no key allowlist while its input does, so an unknown Playwright key reaches the client on stealth only | The named failure mechanism cannot occur. |
| 237faf1...deabac9 | src/engines/chrome_engine.py:139 | Chrome's per-cookie delete-then-add collapses same-name entries within one supplied list, so only the last survives; stealth installs all of them | Refuted on scope and on evidence, though the asymmetry itself is not imaginary. Scope. |
| 237faf1...deabac9 | src/utils.py:95 | An authenticated proxy URL with no explicit port kills the Chrome launch with an opaque %d TypeError, while stealth accepts it | The crash is real but it is upstream FlareSolverr's, on lines the audited range never touched, and the in-range code the finding blames is not the right gate for it. |
| 237faf1...deabac9 | src/geo.py:159 | Concurrent cold misses on one proxy each pay the full lookup | The mechanism is real but the stated failure is not, and the shape is a deliberate tradeoff whose common case is already handled. |
| 237faf1...deabac9 | src/sessions.py:170 | touch, the only mechanism that would refresh last_used, is dead on both levels and writes outside the lock | Refuted as stated: two of the three supporting claims are misreads, and the failure scenario cannot occur in current code. (1) The comment the finding says is contradicted is not about `last_used`. |
| 237faf1...deabac9 | src/flaresolverr_service.py:105 | The error log line and the response `message` field are the one part of the /v1 logging the redaction pass does not cover, and the ledger says othe... | The mechanical half is true and the load-bearing half is not. |
| 237faf1...deabac9 | src/flaresolverr_service.py:480 | The metrics `result="solved"` bucket counts pages the controller itself judged unsolved | The chain is real but nothing in it is Solverr's to answer for, and the cited line is not even the path the finder's own repro takes. What I confirmed by running things. |
| 237faf1...deabac9 | src/engine_fakes.py:161 | Nothing in the browser-free suite can observe media blocking, and the merge rule is a hand-written pair | Every factual claim checks out, but the gap is a documented, scheduled one rather than a defect the range introduced. |
| 237faf1...deabac9 | src/flaresolverr_service.py:469 | Under returnOnlyCookies the unsolved-challenge fallback cannot fire, and the unverified engine is still recorded | The mechanics are right, the defect framing is not. `_looks_challenged` is a documented, deliberately scoped redundant net, not the fallback trigger. |
| 237faf1...deabac9 | src/test_engine_budget.py:56 | Nothing pins the memory, and the browser-free suite writes into the production global under the same host key the one read-path test uses | Refuted: the pollution is real but changes nothing observable, and the read-path assertion cannot fail. |
| 237faf1...deabac9 | src/flaresolverr_service.py:264 | The session surface is a per-engine branch on hardcoded name literals, not an adapter capability | The failure scenario is unreachable and the "asymmetry" is not a divergent data path. 1. No name other than the two literals can reach `_pool_has`. |
| 237faf1...deabac9 | src/config.py:276 | An allow-list entry is never validated as a hostname, and the first one becomes a live default mirror | The mechanism is real but it is not a defect. |
| 237faf1...deabac9 | src/test_session_reaping.py:265 | Five of the seven race windows in ReapRaceOnHandout do not exist, so those subTests assert against a store nothing raced | The arithmetic is right but the conclusion is backwards. |
| 237faf1...deabac9 | src/engines/stealth_engine.py:626 | Byparr's fast re-probe after a click that found no widget has no equivalent; every miss waits the full poll | The "no equivalent" half is false, and the remainder is a parked tuning value. 1. The landed-vs-miss distinction IS implemented. |
| 237faf1...deabac9 | src/dtos.py:44 | Three deprecated /v1 parameters the service explicitly ignores still refuse the request on type | The `headers` exemption is not "deprecated fields are exempt from typing", so the other three are not sibling sites of it. |
